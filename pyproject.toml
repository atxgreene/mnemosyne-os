[project]
name = "mnemosyne-os"
version = "0.1.0"
description = "Local-first cognitive-core developer preview"
readme = "README.md"
license = { file = "LICENSE" }
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "pydantic",
    "starlette",
    "uvicorn",
]

[project.optional-dependencies]
test = ["pytest"]
